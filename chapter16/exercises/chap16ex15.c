/* Chapter 16 Exercise 15 */

/* a) Declear a tag for an enumeration whose values represent the days
 * of the week.
*/

enum day {MON, TUE, WED, THU, FRI, SAT, SUN};

/* b) Use a typedef to define a name for the enumeration of part a). */

typedef enum {MON, TUE, WED, THU, FRI, SAT, SUN} Day;
